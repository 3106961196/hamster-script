# @HAMSTER_MENU@ → bash …/config/tmux/tmux-menu.sh（setup 时替换）
# 键盘触发走 run-shell；鼠标右键由 setup 注入内联 display-menu（-x M -y M）
bind ? run-shell '@HAMSTER_MENU@ help || true'
bind \\ run-shell '@HAMSTER_MENU@ pane #{pane_id} C P || true'
bind -n F9 run-shell '@HAMSTER_MENU@ pane #{pane_id} C P || true'

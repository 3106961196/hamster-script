---
description: hamster-script 的 Shell 必须在真实服务器上按多种运维场景验证，不可只靠本地语法推断
alwaysApply: true
---

# 服务器运维验证（强制）

本仓库是 Linux 运维脚本。**本地写的 Shell 语法与行为都不能默认正确**；改完安装/更新/恢复相关逻辑后，必须在用户的真实服务器上跑通再收工。

## 必须做

1. **真机验证**：用 SSH MCP 连服务器执行；**优先 `ssh-mcp-103` / `project-0-code-ssh-mcp-103`**（183 将到期，仅作备用）。不要只靠 `bash -n`、本地推理或“看起来对”。
2. **语法 + 行为**：既要无报错退出，也要核对关键副作用（目录、`git` 状态、`/usr/local/bin` 命令、tmux、NapCat/QQ 路径等）。
3. **多场景覆盖**：相关改动至少按下面场景想清楚，并实际测会碰到的路径：

| 场景 | 要验证什么 |
|------|------------|
| 干净机首次安装 | 无 `/cs`、无残留时一键安装能否完成 |
| 已安装后正常使用 | 菜单/命令（如 `nt`、`hamster-tmux`）可用 |
| 目录被删后恢复 | `rm -rf /cs`（或安装目录）后，一键安装/恢复能否重建，残留（QQ、systemd、软链）是否可处理 |
| 更新已有安装 | `update` 检测远端、脏工作区、多镜像、无网络时的提示是否正确 |
| 异常 cwd | 在即将被删/重建的目录内执行时，不出现 `getcwd` 类失败 |

4. **调试节奏**：先复现 → 看真实 stderr/exit → 最小修复 → 再在同一场景重跑；涉及安装/卸载时优先用干净或可回滚环境，避免一次改多处无法归因。

## 不要做

- 未上机验证就宣称“没问题”或让用户自己去试关键路径
- 只测一条快乐路径（例如只测“已安装且干净 git”）
- 把服务器密码写进规则、提交或日志；凭据用既有会话/MCP，用后提醒轮换

## 收工标准

向用户汇报时写明：**测了哪些场景、命令/结果摘要、仍未覆盖的风险**。有失败先修再报。
